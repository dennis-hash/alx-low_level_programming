#include "main.h"


/**
 * print_sign - print the sign
 * @n: the number of the signto be printed 
 * Return: 1 if number is greater than zero 0 if number is zero
 * -1 if number is less
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	
}

