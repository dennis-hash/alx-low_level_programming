#include <stdio.h>
#include "main.h"

/**
 * print_to_98 - print fromm input to 98
 * @n: the starting member
 */
void print_to_98(int n)
{
	if (n >= 98)
	}
			printf("%d, ", n--);
		printf("%d\n" n);
	}
	else
	{
		while (n < 98)
			printf("%d, ", n++);
		printf("%d\n", n)
	}
}
